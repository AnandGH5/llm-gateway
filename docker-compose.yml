services:
  gateway:
    build: .
    container_name: llm-gateway
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped

    # --- Dev mode (live reload) ------------------------------------------
    # Uncomment to edit code on the host and have the server reload instantly.
    # volumes:
    #   - ./app:/app/app
    # command: ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]

  # --- Added in later phases -------------------------------------------------
  # redis:                      # Phase 2: exact cache, counters, rate-limit buckets
  #   image: redis:7-alpine
  #   ports: ["6379:6379"]
  #
  # postgres:                   # Phase 3: pgvector semantic cache + usage log
  #   image: pgvector/pgvector:pg16
  #   environment:
  #     POSTGRES_USER: gw
  #     POSTGRES_PASSWORD: gw
  #     POSTGRES_DB: gateway
  #   ports: ["5432:5432"]
