# Minimal Prometheus config to scrape the gateway's /metrics endpoint.
# Enable the prometheus/grafana services in docker-compose.yml to use it.
global:
  scrape_interval: 5s

scrape_configs:
  - job_name: llm-gateway
    static_configs:
      - targets: ["gateway:8000"]
